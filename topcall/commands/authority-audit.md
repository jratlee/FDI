# /authority-audit

Audit every source in the Top Call corpus by authority tier.

Runs the `authority-audit` skill: reports each source's tier, access/paywall
flags, and how many moves it supports. The trust layer, on demand.

- MCP tool: `authority_audit`
- CLI: `node bin/topcall.mjs audit`
