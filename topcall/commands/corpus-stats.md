# /corpus-stats

Show how the Top Call corpus is compounding.

Returns node/edge counts by entity, moves by confidence, ingest-run history, and
all-time confidence upgrades — the proof that the owned corpus grows richer over
time instead of being re-derived each run.

- MCP tool: `corpus_stats`
- CLI: `node bin/topcall.mjs stats`
